import React from "react";

import Container from "@mui/material/Container";

const auth = () => {
  return (
    <Container
      maxWidth="sm"
      style={{
        paddingTop: "6rem",
      }}
    >
      auth
    </Container>
  );
};

export default auth;
